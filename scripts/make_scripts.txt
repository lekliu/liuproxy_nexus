
//1、生成aar脚本

gomobile bind -x -target=android/arm64 -androidapi 21 -o D:\code\github\liuproxy\liuproxy_app\app\libs\liuproxy.aar ./mobile

//2、本地部署到ubuntu服务器
// 一、编译 liuproxy-gateway.tar
docker build -t liuproxy-gateway:latest .
docker save -o liuproxy-gateway.tar liuproxy-gateway:latest

// 二、拷贝到ubuntu系统
root@liuserver:/opt/liuproxy# docker load < liuproxy-gateway.tar
root@liuserver:/opt/liuproxy# docker compose up -d --pull never

//3、验证udp功能
nslookup www.google.com 8.8.8.8


//4、打包上传到 docker-hub
第一步：登录 Docker Hub (必须)
docker login
第二步：构建镜像
docker build -t lekliu/liuproxy-gateway:3.0 .
第三步：推送镜像
docker push lekliu/liuproxy-gateway:3.0
